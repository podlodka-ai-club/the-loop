# OpenRouter API key. Реальный ключ кладётся в .env, который не коммитится.
OPENROUTER_API_KEY=REPLACE_WITH_OPENROUTER_KEY

# Слаг модели OpenRouter. Пока один на оба вызова (observe и infer).
# Разделение vision/text появится, когда слаги будут согласованы.
LOCI_MODEL=REPLACE_WITH_OPENROUTER_SLUG
