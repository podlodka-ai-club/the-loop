# Loci

Loci — фоновый агент, который определяет место съёмки по фотографии и накапливает опыт во внешней
памяти между задачами. Он возвращает координату или набор кандидатов, объясняет вывод и сохраняет
вызовы внутренних инструментов для воспроизводимой оценки.

## Как работает

Общий слепой workflow `locate` используется в трёх режимах:

- production получает ответ на пользовательскую фотографию;
- training после фиксации ответа раскрывает ground truth, формулирует полезные заметки и добавляет
  их в выбранную внешнюю систему памяти;
- evaluation сравнивает один и тот же корпус без памяти и с выбранной привязкой к системе памяти.

Память не изменяется во время inference и evaluation. Ошибки инструментов, retry и terminal failure
фиксируются в контрактах workflow, чтобы результат можно было проверить и воспроизвести.

`memory_snapshot_id` — историческое имя opaque ID привязки к системе и экземпляру памяти
(`xmemory`, Cognee, Mem0 и т.п.), а не версия данных внутри провайдера. Провайдеру не требуется
поддерживать собственные snapshots или переключение между ними; выбор привязки делает оркестратор.

## Как измеряется прогресс

Оценка выполняется на замороженном корпусе. Scorer сравнивает координаты ответа с ground truth по
геодезическому расстоянию WGS84; основной показатель — средняя ошибка, дополнительно фиксируются
медиана и число penalized samples. Положительная разница между baseline и memory-run означает
уменьшение ошибки.

## Benchmark

В репозитории есть работающий benchmark одиночного слепого вызова модели на test-сплите
OSV-5M. Он даёт baseline, с которым сравнивается любой более сложный workflow.

```bash
npm ci
cp .env.example .env      # впишите CEREBRAS_API_KEY
npm run sample            # проверка выборки, без вызовов API
npm run experiment -- --name my-run-1
```

Выборка заморожена файлом `benchmark/samples/osv5m-v1-n200.txt`, поэтому чужая копия
репозитория оценивает те же 200 кадров. Загрузка датасета, запуск Phoenix и сверка с
baseline описаны в [docs/benchmark/reproduce.md](docs/benchmark/reproduce.md).

## Документация

`docs/` — knowledge bundle в формате OKF (Open Knowledge Format), читаемый человеком и агентом.
Точка входа — [docs/index.md](docs/index.md).

| Раздел | О чём |
|---|---|
| [concept](docs/concept.md) | Назначение и общая модель Loci |
| [benchmark](docs/benchmark/index.md) | Выборка OSV-5M, метрики и воспроизведение прогона |
| [data](docs/data/index.md) | Источники изображений и план выгрузки снапшота |
| [research](docs/research/index.md) | Исследования визуальной геолокации и систем агентской памяти |
| [tools](docs/tools/index.md) | Контракты внешних инструментов |
| [workflows](docs/workflows/index.md) | Production, training и evaluation workflow |
| [questions](docs/questions.md) | Нерешённые вопросы архитектуры и процессов |

## Статус

Проектирование workflow и памяти продолжается. Из исполняемого кода готов benchmark в `src/`:
агент, задача, метрики и Phoenix-эксперимент. Ближайшие практические шаги — выгрузка
замороженного снапшота и реализация памяти.
