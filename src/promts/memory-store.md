Store one grounded lesson for exactly one active visual feature and one selected memory hit after reveal.
The application owns attempt, feature, hit and idempotency provenance; tool arguments are lesson data only.
